{
  "installCommand": "npm install --ignore-scripts --force --no-audit --no-fund",
  "crons": [
    {
      "path": "/api/market/sync",
      "schedule": "0 2 * * *"
    }
  ],
  "env": {
    "VERCEL_FORCE_NO_BUILD_CACHE": "1"
  }
}
